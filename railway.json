{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "cron": {
    "auto-topup": {
      "schedule": "*/5 * * * *",
      "command": "curl -X POST -H 'x-internal-secret: ${CRON_SECRET}' https://www.witaline.pl/api/cron/auto-topup"
    },
    "process-jobs": {
      "schedule": "* * * * *",
      "command": "curl -X POST -H 'x-internal-secret: ${CRON_SECRET}' https://www.witaline.pl/api/cron/process-jobs"
    }
  }
}
